<nav class="gcore-navbar {{ $class }}" style="{{ $style }}" {{ $attributes }}>
    <div class="gcore-navbar-brand">
        {{ $brand }}
    </div>
    <div class="gcore-navbar-nav">
        {{ $slot }}
    </div>
</nav>
